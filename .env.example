# Environment variables for dmix-writes

# Base URL for canonical tags, metadata, RSS feed, and sitemap (no trailing slash).
# Optional: defaults to https://blog.dharmikshinde.tech when unset.
SITE_URL=https://blog.dharmikshinde.tech

# Optional: directory for like counts (defaults to ./data, volume-mounted in Docker)
# LIKES_DIR=/data
